<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.developer.project.query.mapper.ProjPostMapper">
    <select id="findAll"
            parameterType="int"
            resultType="ProjPostListResponseDTO">
        SELECT
            proj_post_code,
            proj_post_title,
            user_code,
            created_at,
            updated_at
        FROM proj_post
        ORDER BY created_at DESC
        LIMIT 10 OFFSET #{offset}
    </select>

    <select id="findByCode"
            parameterType="Long"
            resultType="ProjPostResponseDTO">
        SELECT
        proj_post_code,
        proj_post_title,
        proj_post_content,
        proj_url,
        user_code,
        created_at,
        updated_at
        FROM proj_post
        WHERE proj_post_code = #{projPostCode}
    </select>
</mapper>