<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.developer.recruit.query.mapper.RecruitMapper">

    <!-- 채용공고 등록 신청 내역 리스트 조회-->
    <select id="readRecruitList"  resultType="com.developer.recruit.query.dto.RecruitListReadDTO">
        SELECT
        r.recruit_title
        , r.recruit_start
        , r.recruit_end
        , r.recruit_status
        , u.user_id
        FROM recruit r
        JOIN user u
        ON r.user_code = u.user_code
        WHERE r.recruit_appr_status = 'APPROVE'
        ORDER BY recruit_post_date DESC
        LIMIT 10 OFFSET #{offset}
    </select>

    <!-- 채용공고 등록 신청 상세 내역 조회-->
    <select id="readRecruitDetailById"
            parameterType="long"
            resultType="com.developer.recruit.query.dto.RecruitDetailReadDTO">
        SELECT
        r.recruit_title
        , r.recruit_content
        , r.recruit_url
        , r.recruit_start
        , r.recruit_end
        , r.recruit_post_date
        , r.recruit_status
        , u.user_id
        FROM recruit r
        JOIN user u
        ON r.user_code = u.user_code
        WHERE r.recruit_code = #{recruitCode};
    </select>
</mapper>